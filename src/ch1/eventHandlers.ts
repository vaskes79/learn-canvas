// export const eventKeyPressedHandler =
//   (callback: (let: string) => void) => (e: KeyboardEvent) => {
//     console.log(e.code);
//     let letterPressed = String.fromCharCode(e.keyCode);
//     letterPressed = letterPressed.toLowerCase();
//     callback(letterPressed);
//   };
